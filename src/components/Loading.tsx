const Loading = () => {
  return (
    <main className="bg-base-300 z-50 flex justify-center items-center h-screen w-screen">
      <span className="loading loading-spinner loading-xl scale-150 text-secondary"></span>
    </main>
  );
};

export default Loading;
